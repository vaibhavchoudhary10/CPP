#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    string s = to_string(n);
    cout<<s<<endl;
    for (int i = 0; i <= s.size()+1; i++) {
        s.erase(s.begin());
        cout << s << endl;
    }

    return 0;
}
